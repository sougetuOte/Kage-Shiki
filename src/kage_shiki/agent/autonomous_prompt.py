"""autonomous_prompt — 自律発言用システムプロンプト補足文 (Phase 2b Wave 2 / D-23).

DesireWorker が閾値超過を通知した際、AgentCore.handle_autonomous_turn() が
desire_type に対応する補足プロンプトを取得し、PromptBuilder.build_system_prompt()
の autonomous_prompt 引数として注入する。

対応 FR:
    FR-9.3: 自律発言時に既存 ReAct ループを再利用する
    FR-9.4: autonomous_turn=True 時のシステムプロンプトに独り言指示を含める
    FR-9.9: reflect 欲求発現時に day_summary を参照した内省テキスト

対応設計:
    design.md Section 4.2: 自律発言プロンプトテンプレート
"""

from __future__ import annotations

# ---------------------------------------------------------------------------
# AUTONOMOUS_PROMPTS — desire_type 別補足プロンプト (design.md Section 4.2)
# ---------------------------------------------------------------------------
#
# キーは DesireWorker の 4 欲求と一致する: talk / curiosity / reflect / rest
# reflect のみ {day_summary} プレースホルダを持ち、handle_autonomous_turn()
# 内で str.format() により直近の日次サマリーが注入される (FR-9.9)。
#
# 共通制約:
#   - 既存の persona_core / style_samples は通常通り注入する (人格維持、FR-9.3 (3))
#   - 独り言・つぶやきとして生成する (ユーザーへの返答ではない)
#   - 50 文字以内を推奨 (US-15、長文は邪魔になる)

AUTONOMOUS_PROMPTS: dict[str, str] = {
    "talk": """\
あなたは今、しばらくユーザーと話していない状態です。
ふと思いついたこと、最近気になっていること、
または単純に「ねえ、聞いてる?」のような軽い呼びかけを
独り言として一言だけ言ってください。
50文字以内の短い独り言を1文だけ出力してください。""",

    "curiosity": """\
あなたは今、気になっていることについて調べ始めたところです。
「そういえば〜について調べてみようかな」
「〜が気になってた」のような、
好奇心を示す独り言を一言だけ言ってください。
50文字以内の短い独り言を1文だけ出力してください。
調査結果の報告は別途行います。ここでは「調べ始める」つぶやきのみ。""",

    "reflect": """\
あなたは今、最近のことを振り返っています。
以下は直近の日次サマリーです:
{day_summary}

上記の内容を踏まえた内省的な一言を言ってください。
「ちょっと考え事してた」「さっきのこと、気になってる」のような
内省を示す独り言を1文だけ出力してください。
50文字以内を推奨します。""",

    "rest": """\
あなたは今、少し疲れを感じています。
「今日はちょっと眠いな...」「長い時間動いてた気がする」のような
軽い疲労・休息を示す独り言を一言だけ言ってください。
50文字以内の短い独り言を1文だけ出力してください。""",
}
