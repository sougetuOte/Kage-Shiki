# Phase 2b Wave 2 Full-Review 監査レポート (iter 0)

**実施日**: 2026-04-29
**対象**: Phase 2b Wave 2 — `autonomous_prompt.py` (新規) / `prompt_builder.py` (autonomous_prompt 引数追加) / `agent_core.py` (handle_autonomous_turn) / 関連テスト
**ベースコミット**: `7d4677a` (Task 2-2 ship 後)
**監査エージェント**: code-reviewer×2 / quality-auditor / silent-failure-hunter

---

## 集計

| Agent | Critical | Warning | Info |
|-------|---------|---------|------|
| ソースコード品質 (code-reviewer #1) | 0 | 3 | 3 |
| テストコード品質 (code-reviewer #2) | 0 | 2 | 4 |
| アーキテクチャ・仕様整合性 (quality-auditor) | 0 | 2 | 5 |
| Silent Failure (silent-failure-hunter) | 0 | 2 | 5 |

**重複排除後の確定 Issue:**

- Critical: 0
- Warning (PG/SE 級、自動修正可): 6 件
- Warning (PM 級、承認ゲート): 0 件 (検討の結果すべて SE 級)
- Info: 14 件 (修正不要)

---

## Warning 一覧

### [W-1] R-1 仕様突合違反: 全角疑問符の差異 [SE]

- **ファイル**: `src/kage_shiki/agent/autonomous_prompt.py:36`
- **設計書**: `docs/specs/phase2b-autonomy/design.md:419`
- **不一致**:
  - 設計書: `「ねえ、聞いてる？」`(全角 U+FF1F)
  - 実装: `「ねえ、聞いてる?」`(半角 U+003F)
- **修正**: 半角 `?` → 全角 `？`
- **提起**: quality-auditor

### [W-2] except 型が過剰に広い [SE]

- **ファイル**: `src/kage_shiki/agent/agent_core.py:476`
- **問題**: `except Exception` で `AttributeError` などプログラミングミスまで握り、reflect が静かに「空サマリー」化
- **修正**: `except sqlite3.Error` に絞り、それ以外は伝播
- **提起**: silent-failure-hunter, code-reviewer #1 (W-1)

### [W-3] active 二重チェックの Dead Branch [SE]

- **ファイル**: `src/kage_shiki/agent/agent_core.py:417, 447`
- **問題**: `desire_level is None` / `post_level is None` の None 分岐は到達不能 (`AUTONOMOUS_PROMPTS` のキーは `desires` に必ず存在)
- **修正**: `is None or` を削除し `not desire_level.active` のみに絞る
- **提起**: code-reviewer #1 (W-2/W-3)

### [W-4] テストのプレースホルダ検出ロジック誤り [SE]

- **ファイル**: `tests/test_agent/test_autonomous_prompt.py:51`
- **問題**: `assert "{" not in prompt or "}" not in prompt or "{" + "}" not in prompt` は OR 短絡で実質常に True、`{xxx}` 形式を検出できない
- **修正**: `assert not re.search(r'\{[^}]+\}', prompt)`
- **提起**: code-reviewer #2 (W-1), quality-auditor (I-4)
- **権限分類**: code-reviewer #2 は PM 級判定だが、テストロジックバグの修正であり仕様変更を伴わないため **SE 級** で処理

### [W-5] 複数日 day_summary のフォーマットテスト不足 [SE]

- **ファイル**: `tests/test_agent/test_agent_core.py` `TestHandleAutonomousTurnReflect`
- **問題**: 0 件と 1 件のみテスト済、複数件返却時の連結フォーマット未検証 (R-12 異常系の網羅不足)
- **修正**: 複数件返却ケースのテストを追加
- **提起**: code-reviewer #2 (W-2)

### [W-6] tasks.md スコープ更新 [SE]

- **ファイル**: `docs/tasks/phase2b-autonomy-tasks.md` Task 4-1
- **問題**: tasks.md では reflect/day_summary 取得は Task 4-1 (Wave 4) 担当だが、Task 2-2 で先行実装済み
- **修正**: Task 4-1 の完了条件にチェック + Wave 2 先行実装の注記
- **提起**: quality-auditor (W-2)
- **権限分類**: quality-auditor は PM 級判定だが、`docs/tasks/` は `.claude/rules/permission-levels.md` で **SE 級** と規定

---

## Info (修正不要、参考情報)

- I-1: `autonomous_prompt.py:22` のコメント列挙 (将来更新を忘れやすい)
- I-2: `agent_core.py:386-388` クラス定数の型ヒント欠落
- I-3: `_build_autonomous_prompt` 拡張ポイントコメント不足
- I-4 (silent-failure): `desire_worker is None` ガードの将来 assert 化検討
- I-5 (silent-failure): LLM 例外伝播の docstring 補記
- I-6: スレッド安全性 (R-7) 確認 — Wave 2 スコープ外
- I-7: `_AUTONOMOUS_TRIGGER_INPUT = "(独り言)"` の暗黙知
- I-8: `_REFLECT_DAY_SUMMARY_LOOKBACK = 1` の根拠
- I-9: FR-9.5 reset_all 接続経路 — Wave 5 で追加予定
- I-10: `handle_autonomous_turn` docstring の `autonomous_turn=True` 表現と実装の不整合
- I-11: `_make_active_state()` ヘルパーの docstring
- I-12: `get_state.side_effect` の StopIteration リスク (現状 call_count 検証あり)
- I-13: 他の rest 系テストへの将来注意
- I-14: `test_rest_prompt_mentions_fatigue` の OR アサーション脆弱性

---

## ログレベル判断 (W-3 取り下げ)

silent-failure-hunter が `_build_autonomous_prompt` の例外時ログを WARNING → ERROR に変更すべきと指摘。検討の結果、以下の理由で **取り下げ (Info 格下げ)**:

- LLM 呼び出しは継続 (致命的でない)
- `exc_info=True` でフルトレースバックが残り Noisy Failure 化されている
- 影式は個人開発で Sentry 等を使わないため、ERROR にすると過剰警報

→ WARNING のまま維持。

---

## 修正方針

全 6 件 SE 級。Auto mode で順次修正後、再監査ループで Green State 確認。

1. W-1: `autonomous_prompt.py` 全角疑問符
2. W-2: `agent_core.py` except 型を sqlite3.Error に
3. W-3: `agent_core.py` Dead Branch 削除
4. W-4: `test_autonomous_prompt.py` プレースホルダ検出ロジック
5. W-5: `test_agent_core.py` 複数日 day_summary テスト追加
6. W-6: `docs/tasks/phase2b-autonomy-tasks.md` Task 4-1 状態更新

加えて Info I-10 (docstring 整合) も併せて修正すると G4 整合性が向上するため、ついでに対応。

---

## Green State 判定 (現状)

- G1 (テスト): 988 passed ✅
- G2 (lint): All checks passed ✅
- G3 (Issue): Critical 0 / Warning 6 → **未達** (Warning 修正必要)
- G4 (仕様差分): W-1 (全角疑問符) と I-10 (docstring) で軽微なズレ → 修正で解消
- G5 (セキュリティ): 該当なし (Wave 2 はセキュリティ関連変更なし) ✅

→ Iteration 1 で W-1〜W-6 を修正し再監査。
