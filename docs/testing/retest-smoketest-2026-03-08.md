# スモークテスト再検証チェックリスト (2026-03-08)

**目的**: 2026-03-07 スモークテストで発見・修正した12件の再検証
**前提**: `git push` 済み（fd28418）、pytest 722 passed / 92% cov / ruff clean
**参照**: `docs/memos/feedback-smoketest-2026-03-07.md`（修正内容一覧）

---

## 準備

```bash
# 1. クリーンな状態で依存インストール
pip install -e .

# 2. 自動テスト通過を確認
pytest --tb=short -q

# 3. persona を未凍結状態にする（ウィザードフロー確認のため）
#    ~/.kage_shiki/config.toml で persona_frozen = false に設定
#    または ~/.kage_shiki/persona/ を一時リネームして初回起動を再現
```

---

## Part A: ウィザードフロー（修正 #4, #7, #10, #12）

`persona_frozen = false` の状態で起動する。

- [ ] **A-1**: `python -m kage_shiki` でウィザードが起動する
- [ ] **A-2**: キーワード入力で日本語区切り文字が使える（修正 #7）
  - 入力例: `優しい、猫耳、ツンデレ` （読点区切り）
  - 入力例: `元気 明るい 甘えん坊` （スペース区切り）
  - 期待: いずれも正しく分割されて候補生成に進む
- [ ] **A-3**: LLM がリスト値を返してもエラーにならない（修正 #4）
  - 期待: 凍結まで進行し TypeError が出ない
- [ ] **A-4**: 凍結完了後に「再起動してください」ダイアログが表示される（修正 #10）
- [ ] **A-5**: プレビュー会話でユーザー名が正しく表示される（修正 #12）
  - 期待: 「あなた」ではなく設定した名前（c3_second_person）が表示される

---

## Part B: メインウィンドウ（修正 #2, #8, #9）

`persona_frozen = true` の状態で起動する（ウィザード完了後 or 手動設定）。

- [ ] **B-1**: ウィンドウサイズが適切（修正 #9）
  - 期待: 高さが以前より大きくなっている（デフォルト 450px）
- [ ] **B-2**: テキスト入力して送信 → 自分の発言がチャット欄に表示される（修正 #8）
  - 期待: `[ユーザー名] メッセージ内容` の形式で表示
- [ ] **B-3**: キャラの応答がチャット欄に追記表示される（修正 #8）
  - 期待: ユーザー発言の下にキャラ応答が追記される（上書きではない）
- [ ] **B-4**: 突っつきボタン(♪)をクリック → キャラが反応する（修正 #2）
  - 期待: ♪ボタン押下後、数秒以内にキャラのリアクションが表示される

---

## Part C: トレイ操作（修正 #3, #5, #11）

- [ ] **C-1**: トレイアイコン左クリック → ウィンドウ表示/非表示がトグルする（修正 #3）
  - 手順: 左クリックで非表示 → 再度左クリックで表示
- [ ] **C-2**: トレイ右クリック → 「最小化」メニューが存在する（修正 #3）
- [ ] **C-3**: トレイ右クリック → 「ウィザード起動」メニューが存在する（修正 #11）
- [ ] **C-4**: 「ウィザード起動」選択 → 確認ダイアログ → ウィザードが起動する（修正 #11）
  - 期待: persona の .bak バックアップが作成される
- [ ] **C-5**: ウィザード完了後、トレイエラーが出ない（修正 #5）

---

## Part D: シャットダウン + 2回目起動（修正 #6）

- [ ] **D-1**: トレイ「終了」→ ウィンドウ消失、プロセス残存なし（修正 #6）
  - 確認: タスクマネージャーで python.exe が残っていないこと
- [ ] **D-2**: ログに ERROR / CRITICAL がないこと
  - 確認: `~/.kage_shiki/logs/kage_shiki.log`
- [ ] **D-3**: 2回目起動が正常に動作する
  - 期待: UNIQUE 制約エラー等なし
- [ ] **D-4**: 2回目起動で前回の記憶を踏まえた挨拶がある
  - 期待: 初回とは異なる発言（前回セッション内容への言及）
- [ ] **D-5**: 2回目もシャットダウンが正常に完了する

---

## Part E: FTS5 サニタイズ（修正 #1）

- [ ] **E-1**: 対話中に `[` や `]` を含む発言をしてもエラーにならない（修正 #1）
  - 入力例: `[テスト]についてどう思う？`
  - 期待: FTS5 構文エラーが発生せず正常に応答が返る

---

## 合否判定

| Part | 項目数 | 合否 |
|------|--------|------|
| A. ウィザード | 5 | |
| B. メインウィンドウ | 4 | |
| C. トレイ操作 | 5 | |
| D. シャットダウン+再起動 | 5 | |
| E. FTS5 | 1 | |
| **合計** | **20** | |

全項目パスで Phase 2a 完了判定可。
失敗項目がある場合はログを添えて報告し、修正サイクルに戻る。
